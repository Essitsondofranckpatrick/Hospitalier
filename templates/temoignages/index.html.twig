{% extends 'base-back.html.twig' %}

{% block title %}Temoignages index{% endblock %}

{% block body %}
    <h1>Temoignages index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Email</th>
                <th>Message</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for temoignage in temoignages %}
            <tr>
                <td>{{ temoignage.id }}</td>
                <td>{{ temoignage.firstname }}</td>
                <td>{{ temoignage.email }}</td>
                <td>{{ temoignage.message }}</td>
                <td>
                    <a href="{{ path('app_temoignages_show', {'id': temoignage.id}) }}">show</a>
                    <a href="{{ path('app_temoignages_edit', {'id': temoignage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_temoignages_new') }}">Create new</a>
{% endblock %}
