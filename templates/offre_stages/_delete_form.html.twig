<form method="post" action="{{ path('app_offre_stages_delete', {'projet': offre_stage.projet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_stage.projet) }}">
    <button class="btn">Delete</button>
</form>
