{% extends 'base-back.html.twig' %}

{% block title %}RealiserProjet{% endblock %}

{% block body %}
    <h1>RealiserProjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Duree</th>
                <td>{{ realiser_projet.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_realiser_projet_index') }}">back to list</a>

    <a href="{{ path('app_realiser_projet_edit', {'projet': realiser_projet.projet}) }}">edit</a>

    {{ include('realiser_projet/_delete_form.html.twig') }}
{% endblock %}
