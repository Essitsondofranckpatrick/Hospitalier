<form method="post" action="{{ path('app_realiser_projet_delete', {'projet': realiser_projet.projet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realiser_projet.projet) }}">
    <button class="btn">Delete</button>
</form>
