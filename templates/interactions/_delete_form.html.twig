<form method="post" action="{{ path('app_interactions_delete', {'id': interaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interaction.id) }}">
    <button class="btn">Delete</button>
</form>
