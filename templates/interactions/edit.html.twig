{% extends 'base-back.html.twig' %}

{% block title %}Edit Interactions{% endblock %}

{% block body %}
    <h1>Edit Interactions</h1>

    {{ include('interactions/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_interactions_index') }}">back to list</a>

    {{ include('interactions/_delete_form.html.twig') }}
{% endblock %}
