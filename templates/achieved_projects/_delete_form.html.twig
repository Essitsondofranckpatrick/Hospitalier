<form method="post" action="{{ path('app_achieved_projects_delete', {'id': achieved_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achieved_project.id) }}">
    <button class="btn">Delete</button>
</form>
